# Copy to .env and fill in. .env is gitignored — never commit real values.

# Cloudflare (for `wrangler pages deploy` and R2 once enabled)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=a67eceeb4b89d2d4171ed209e87c9456

# R2 (only after enabling R2 in the dashboard) — S3-compatible keys for the upload script
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=linguaguessr-clips
